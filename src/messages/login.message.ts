/**
 * --- WhatsApp Login Message Template ---
 */

/**
 * Returns the highly professional WhatsApp template for account login
 */
export function getLoginTemplate(code: string): string {
  return `*Clearvue Login*\n_System Authorization_\n\n*Code:* \`\`\`${code}\`\`\``;
}

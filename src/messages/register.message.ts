/**
 * --- WhatsApp Register Message Template ---
 */

/**
 * Returns the highly professional WhatsApp template for account registration
 */
export function getRegisterTemplate(code: string): string {
  return `*Clearvue Registration*\n_System Authorization_\n\n*Code:* \`\`\`${code}\`\`\``;
}
